{
  "name": "eakigbo",
  "version": "2.0.0",
  "private": true,
  "nodemonConfig": {
    "ignore": [
      "./public"
    ]
  },
  "scripts": {
    "start": "node ./app",
    "dev": "NODE_ENV=development nodemon",
    "build": "parcel build src/main.js src/styles.css --out-dir public/dist",
    "deploy": "NODE_ENV=production yarn build && yarn start"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.5",
    "cross-var": "^1.1.0",
    "nodemon": "^1.18.4"
  },
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.18.2",
    "compression": "^1.7.1",
    "cookie-parser": "^1.4.3",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "express-handlebars": "^3.0.0",
    "marked": "^0.3.6",
    "morgan": "^1.9.0",
    "parcel-bundler": "^1.9.7",
    "serve-favicon": "^2.4.5"
  }
}
